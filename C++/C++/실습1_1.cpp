#include <string>
#include <iostream>
#include "½Ç½À1.h"

string Comma::getComma(string input)
{
	int length;
	length = this->input.length();
}
void Comma::setComma(string input)
{
	this->input = input;
}